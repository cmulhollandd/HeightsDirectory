# Uncomment the next line to define a global platform for your project
platform :ios, '11.3'

target 'HeightsDirectory' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HeightsDirectory
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Valet'

  target 'HeightsDirectoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HeightsDirectoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
